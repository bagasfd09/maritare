// Shared admin display types.
//
// The sample/mock data that used to live here (KPIs, weddings, transactions,
// templates, packages, promos, team) has been REMOVED — every admin screen now
// reads real data from `src/server/queries/admin.ts`. Only small shared display
// types remain.

export type PackageName = "Silver" | "Gold" | "Platinum";
